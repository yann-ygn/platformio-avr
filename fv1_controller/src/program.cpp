#include <Arduino.h>

#include "program.h"

program programs[7] =
{
    //programs[0] : simple digital delay
    {
        .m_id = 0,
        .m_delayEffect = 1,
        .m_maxInterval = 1000,
        .m_tapEnabled = 1,
        .m_pot0Enabled = 1,
        .m_pot1Enabled = 1,
        .m_pot2Enabled = 1
    },

    //programs[1] : 
    {
        
    }
};